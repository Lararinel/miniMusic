// 字符串截取
var substring = function (str, max = 14) {
  if (str.length == 0 || str == undefined) {
    return;
  }
  if (str.length > max) {

    console.log(max, str.substring(0, max))
    return str.substring(0, max) + '...';
  } else {
    return str;
  }
}

module.exports.substring = substring;